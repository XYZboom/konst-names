pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "konst-names"

dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
        }
    }
}